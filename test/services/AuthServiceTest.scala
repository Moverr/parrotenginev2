package services

import org.scalatestplus.mockito.MockitoSugar
import org.scalatestplus.play.PlaySpec

class AuthServiceTest extends PlaySpec with MockitoSugar {
  "Auth service" should  {
    "returun profile when login is succesful" in {
   /*   val userDao = mock[UserDao]
      userDao.getUserByName("sddd")



        val authService = new AuthService(userDao)
      authService.validate(null) must assertThrows(NullPointerException)
*/
    }
  }
}
